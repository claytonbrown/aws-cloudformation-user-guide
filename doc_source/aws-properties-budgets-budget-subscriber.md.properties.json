{
  "budgets.budget.subscriber.CfnAbout": "The `Subscriber` property type specifies who to notify for a Billing and Cost Management budget notification. The subscriber consists of a subscription type, and either an Amazon SNS topic or an email address.\n\nFor example, an email subscriber would have the following parameters:\n+ A `subscriptionType` of `EMAIL`\n+ An `address` of `example@example.com`",
  "budgets.budget.subscriber.CfnType": "AWS::Budgets::Budget.Subscriber",
  "budgets.budget.subscriber.CfnDocsKey": "aws-properties-budgets-budget-subscriber",
  "budgets.budget.subscriber.Address": {
    "Description": "The address that AWS sends budget notifications to, either an SNS topic or an email.  When you create a subscriber, the value of `Address` can't contain line breaks.",
    "Required": "Yes",
    "Type": "String",
    "UpdateRequires": "[No interruption](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-updating-stacks-update-behaviors.html#update-no-interrupt)"
  },
  "budgets.budget.subscriber.SubscriptionType": {
    "Description": "The type of notification that AWS sends to a subscriber.",
    "Required": "Yes",
    "Type": "String",
    "Allowed values": "EMAIL | SNS",
    "UpdateRequires": "[No interruption](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-updating-stacks-update-behaviors.html#update-no-interrupt)\n\n#"
  }
}
