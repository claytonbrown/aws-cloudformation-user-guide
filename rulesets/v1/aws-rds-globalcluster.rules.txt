# #AWS::RDS::GlobalCluster DeletionProtection  == True |OR| AWS::RDS::GlobalCluster DeletionProtection  == False << Enforce allowed values [True | False]
# #AWS::RDS::GlobalCluster StorageEncrypted  == True |OR| AWS::RDS::GlobalCluster StorageEncrypted  == False << Enforce allowed values [True | False]
# AWS::RDS::GlobalCluster DeletionProtection  == /False/ <<  False is an expected value for AWS::RDS::GlobalCluster DeletionProtection
# AWS::RDS::GlobalCluster DeletionProtection  == /True/ <<  True is an expected value for AWS::RDS::GlobalCluster DeletionProtection
# AWS::RDS::GlobalCluster DeletionProtection  == False <<  False is expected  safe default value
# AWS::RDS::GlobalCluster DeletionProtection  == True <<  True is expected safe default value
# AWS::RDS::GlobalCluster DeletionProtection IN [True,False] << Enforcing Allowed Values only
# AWS::RDS::GlobalCluster StorageEncrypted  == /False/ <<  False is an expected value for AWS::RDS::GlobalCluster StorageEncrypted
# AWS::RDS::GlobalCluster StorageEncrypted  == /True/ <<  True is an expected value for AWS::RDS::GlobalCluster StorageEncrypted
# AWS::RDS::GlobalCluster StorageEncrypted  == False <<  False is expected  safe default value
# AWS::RDS::GlobalCluster StorageEncrypted  == True <<  True is expected safe default value
# AWS::RDS::GlobalCluster StorageEncrypted IN [True,False] << Enforcing Allowed Values only